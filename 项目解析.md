# 项目解析

本文档基于当前目录结构 `D:\项目2-26` 编写。当前有效项目位于根目录下：

```text
D:\项目2-26
├── project1/   # Rockchip 多路视频解码 + RKNN 推理 + 拼接/推流示例
├── project2/   # GB/T 28181 SIP 信令 + H.264 PS/RTP 推流示例
├── AGENTS.md
└── 项目解析.md
```

此前的 `项目/项目/项目/project1` 已迁移到根目录 `project1/`，嵌套旧副本 `项目/项目/项目/project2` 已删除。IDE 中如果仍打开旧路径文件，需要重新从根目录下的 `project1/` 或 `project2/` 打开。

> 两个项目都面向 ARM Linux / Rockchip 平台，不适合直接在 Windows/x86 上运行。它们依赖 Rockchip MPP、RGA、RKNN 或 SIP 相关本地动态库。

---

## 一、project1：多路视频解码与 RKNN 智能分析

### 1. 项目定位

`project1/` 是一个 Rockchip 平台的视频 AI 分析示例集合，核心目标是：

```text
多路 MP4/RTSP 视频输入
  -> FFmpeg 读流/读包
  -> Rockchip MPP 硬解码
  -> RGA / OpenCV 图像转换与缩放
  -> RKNN 多模型推理
  -> 检测结果融合与绘制
  -> 多路画面拼接
  -> MPP 硬编码 H.264
  -> FFmpeg 封装 RTMP 推流
```

它更像一个“多路视频智能分析 + 可视化输出”的 demo 工程。

### 2. 顶层目录

```text
project1/
├── demo_multhread_decode/                 # 空目录，当前没有实际代码
├── demo_multhread_decode_infer/           # 早期版本：多路解码 + 单模型推理 + 本地显示
├── demo_multhread_decode_infer_mulmodel/  # 当前最完整版本：多模型推理 + 融合 + 拼接 + RTMP 推流
├── ffmpeg-lite/                           # 本地 FFmpeg 精简库
├── ffmpeg7-1/                             # FFmpeg 7.x 库/头文件备份
├── model/                                 # RKNN 模型文件
├── mpp/                                   # Rockchip MPP 库和头文件
├── opencv4-1/                             # 本地 OpenCV
├── rga/                                   # Rockchip RGA 库和头文件
├── rknn/                                  # RKNN runtime/API
├── 1.mp4 / 2.mp4 / 3.mp4 / 4.mp4          # 本地测试视频
├── lib.sh                                 # 解压 FFmpeg/OpenCV 库脚本
└── 流程图.png
```

### 3. 当前主版本：demo_multhread_decode_infer_mulmodel

主入口：

```text
project1/demo_multhread_decode_infer_mulmodel/main.cpp
```

构建文件：

```text
project1/demo_multhread_decode_infer_mulmodel/CMakeLists.txt
```

核心源码结构：

```text
demo_multhread_decode_infer_mulmodel/
├── main.cpp
├── include/
│   ├── stream_loader.h             # 视频输入与多路 StreamLoader 管理
│   ├── mpp_decoder.h               # MPP 硬解码封装
│   ├── rknnPool.hpp                # RKNN 模型加载、推理封装
│   ├── postprocess.h               # YOLO 类模型后处理接口
│   ├── detection_fusion_manager.h  # 多模型检测结果融合
│   ├── streaming_manager.h         # RTMP/RTSP 推流管理
│   ├── mpp_encoder.h               # MPP 硬编码封装
│   ├── m_buffer.hpp                # 每路视频帧缓冲
│   └── ThreadPool.hpp              # 简单线程池
└── lib/
    ├── stream_loader.cpp
    ├── mpp_decoder.cpp
    ├── rknnPool.hpp 中实现部分
    ├── postprocess.cpp
    ├── detection_fusion_manager.cpp
    ├── streaming_manager.cpp
    └── mpp_encoder.cpp
```

### 4. 主流程

`main.cpp` 的执行逻辑：

```text
main(argc, argv)
  -> manager.num_stream = argv[1]
  -> 初始化 StreamingManager
  -> startStreaming()
  -> for 每一路视频:
       manager.load_stream(i)
       创建 person / helmet / callplay RKNN 模型实例
       启动 rknn_infer 线程
  -> 启动 combineImage 线程
```

运行时需要传入视频路数，例如：

```bash
./myDemo 4
```

默认视频源在 `StreamLoaderManager` 里写死为本地文件：

```text
../../1.mp4
../../2.mp4
../../3.mp4
../../4.mp4
../../2.mp4
../../3.mp4
```

最多预设 6 路，拼接布局也是按 6 路设计。

### 5. 解码链路

相关文件：

```text
include/stream_loader.h
lib/stream_loader.cpp
include/mpp_decoder.h
lib/mpp_decoder.cpp
include/m_buffer.hpp
```

流程：

```text
StreamLoader::open()
  -> avformat_open_input()
  -> avformat_find_stream_info()
  -> 找到 video stream
  -> 判断 H.264/H.265
  -> MppDecoder::Init()
  -> H.264 时配置 h264_mp4toannexb bitstream filter

StreamLoader::operator()()
  -> 循环 read_frame()
  -> FFmpeg av_read_frame()
  -> 必要时 bitstream filter 转 Annex-B
  -> decoder.Decode(packet)

MppDecoder::Decode()
  -> mpp_packet_set_data()
  -> mpi->decode_put_packet()
  -> mpi->decode_get_frame()
  -> callback()
```

解码输出 callback：

```text
mpp_decoder_frame_callback()
  -> 从 MPP frame buffer 取 NV12/YUV 数据
  -> 优先 RGA: NV12 -> BGR
  -> RGA 失败 fallback 到 OpenCV cvtColor
  -> 写入 Mbuffer.img
```

`Mbuffer` 是每路视频的最新帧缓冲：

```cpp
struct Mbuffer {
    cv::Mat img;
    std::mutex mtx;
    int frame_interval_ms;
    bool throttle;
    std::vector<uint8_t> yuv_work;
    cv::Mat bgr_work;
};
```

本地 MP4 文件会根据源帧率做限速，避免读取过快。

### 6. 推理链路

相关文件：

```text
include/rknnPool.hpp
include/postprocess.h
lib/postprocess.cpp
main.cpp
```

`rknn_lite` 封装一个 RKNN 模型实例，负责：

```text
load_model()
  -> rknn_init()
  -> rknn_set_core_mask()
  -> rknn_query 输入输出属性
  -> 设置输入参数

rknn_lite::interf()
  -> BGR -> RGB
  -> resize 到模型输入尺寸
  -> rknn_inputs_set()
  -> rknn_run()
  -> rknn_outputs_get()
  -> post_process()
  -> rknn_outputs_release()
```

当前多模型主流程中，每路视频创建：

```text
person_relu.rknn    -> RKNN_NPU_CORE_0
helmet_relu.rknn    -> RKNN_NPU_CORE_1
callplay_relu.rknn  -> RKNN_NPU_CORE_2
tired_relu.rknn     -> 已预留，但当前 main.cpp 中 p3 = nullptr，未启用
```

`rknn_infer()` 每路一个线程，线程内部又用 `ThreadPool pool(4)` 并行跑不同模型：

```text
rknn_infer()
  -> 从该路 Mbuffer.img 取最新帧
  -> 分别交给 person / helmet / callplay 模型
  -> ThreadPool 并发推理
  -> post_process 得到检测结果
  -> DetectionFusionManager 融合
  -> 绘制融合结果
  -> 写入 images[i]
```

代码设置了跳帧推理：

```text
INFER_INTERVAL = 2
```

含义是每隔 2 帧做一次完整推理，中间帧复用上一帧融合结果，以降低 NPU 压力。

### 7. 检测融合

相关文件：

```text
include/detection_fusion_manager.h
lib/detection_fusion_manager.cpp
```

融合结构：

```text
FusedDetection
  -> class_id
  -> class_name
  -> bbox
  -> confidence
  -> source_models
  -> model_confidences
  -> timestamp
```

融合配置：

```text
IoU threshold       = 0.5
IoM threshold       = 0.3，主要用于 person + helmet 这类嵌套关系
confidence threshold = 0.3
enable NMS          = true
model weights       = person 1.0 / helmet 0.9 / tired 0.8 / callplay 0.7
```

职责：

```text
多模型结果
  -> 过滤低置信度
  -> 根据 IoU / IoM 合并相关框
  -> 加权融合 bbox 和 confidence
  -> NMS 去重
  -> drawFusedDetections() 绘制到图像
```

### 8. 多路画面拼接

相关函数：

```text
main.cpp -> combineImage()
```

拼接画布：

```text
combinedImage: 1280 x 1080
单路 tile:     640 x 360
布局:          2 列 x 3 行
最大适配:      6 路
```

流程：

```text
images[i]
  -> RGA imresize 到 640x360
  -> RGA 失败 fallback OpenCV resize
  -> copyTo 到 combinedImage 的对应区域
  -> 生成 StreamingData
  -> streaming_manager.addStreamingData()
```

`combineImage()` 按目标 24fps 输出合成图，并在没有新帧时复用上一张合成图，以保持推流连续。

### 9. 推流链路

相关文件：

```text
include/streaming_manager.h
lib/streaming_manager.cpp
include/mpp_encoder.h
lib/mpp_encoder.cpp
```

`StreamingManager` 管理一个推流队列和一个推流线程：

```text
addStreamingData()
  -> 将合成帧放入 streaming_queue_
  -> 队列超过 10 帧时丢弃最旧帧

startStreaming()
  -> 启动 streamingWorker()

streamingWorker()
  -> 从队列取帧
  -> 可选绘制检测统计
  -> resize 到推流分辨率
  -> sendRTMPFrame()
```

RTMP 推流使用 MPP 硬编码，不用 FFmpeg 软件编码：

```text
BGR frame
  -> MppEncoder::EncodeFrame()
  -> RGA BGR -> YUV420P，失败则 OpenCV fallback
  -> MPP encode H.264
  -> FFmpeg av_interleaved_write_frame()
  -> RTMP/FLV 输出
```

默认推流配置在 `main.cpp`：

```text
rtmp_url = rtmp://192.168.137.1/live/livestream
width    = 1280
height   = 720
fps      = 24
bitrate  = 2000000
```

注意：`combineImage()` 合成的是 `1280x1080`，但推流配置是 `1280x720`，所以推流线程会再 resize 一次。这会改变 6 路拼接画面的纵向比例。

### 10. project1 构建与依赖

`demo_multhread_decode_infer_mulmodel/CMakeLists.txt` 使用本地库路径：

```text
../ffmpeg-lite
../opencv4-1
../rknn
../mpp
../rga
```

生成目标：

```text
myDemo
```

链接库：

```text
libavcodec.so
libavformat.so
libavutil.so
libopencv_*.so
librockchip_mpp.so
librga.so
librknn_api.so
librknnrt.so
```

典型构建方式：

```bash
cd project1/demo_multhread_decode_infer_mulmodel
mkdir -p build
cd build
cmake ..
make -j$(nproc)
./myDemo 4
```

如本地库未解压，可先在 `project1/` 下运行：

```bash
bash lib.sh
```

### 11. project1 注意事项

- 运行平台应是 Rockchip ARM Linux，且需要 MPP/RGA/RKNN 驱动环境。
- `demo_multhread_decode/` 当前为空，不是有效 demo。
- 最完整代码在 `demo_multhread_decode_infer_mulmodel/`。
- RTMP 地址写死在 `main.cpp`，部署时需要改为实际地址。
- 输入视频源写死在 `StreamLoaderManager::urls`，默认使用本地 `1.mp4` 到 `4.mp4`。
- `main.cpp` 未检查 `argc`，如果不传路数参数会异常。
- `tired_relu.rknn` 模型存在，但当前多模型主流程没有启用。
- IDE 旧标签页 `项目/项目/项目/project1/...` 已不是有效路径，应改用 `project1/...`。

---

## 二、project2：GB/T 28181 视频推流

### 1. 项目定位

`project2/` 是一个 GB/T 28181 国标视频推流 demo，目标是把本地 MP4 视频通过 Rockchip MPP 硬编码为 H.264，再封装为 PS/RTP，通过 UDP 推给 GB28181 平台，同时用 eXosip2/osip2 处理 SIP 注册、查询、点播邀请等信令。

核心链路：

```text
main.cc
  -> pushToGB28181()
      -> SipThread()
          -> REGISTER / MESSAGE / SUBSCRIBE / INVITE / ACK / BYE
          -> ACK 后启动 encode() 和 work()

  -> decode_from_mp4()
      -> OpenCV VideoCapture 读本地 MP4
      -> set_Mat(frame)
      -> matData 队列

encode()
  -> cv::Mat BGR
  -> resize 1280x720
  -> BGR -> YUV420 I420
  -> MPP H.264 编码
  -> h264data 队列

work()
  -> H.264
  -> PS/PES/RTP 打包
  -> UDP 发送到平台
```

### 2. 顶层目录

```text
project2/
├── CMakeLists.txt
├── main.cc
├── push.cc / push.h
├── decode.cc / decode.h
├── ps_rtp.cc / ps_rtp.h
├── gb28181.cc / gb28181.h
├── config.cc / config.h
├── frame_queue.h
├── xml_utils.cc / xml_utils.h
├── test.mp4
├── scripts/
│   ├── install_sip_local.sh
│   ├── run_demo.sh
│   ├── fix_sip_rpath.sh
│   └── download_sip_sources.sh
├── install/             # 本地 osip2/eXosip2 安装结果
├── MPP/                 # Rockchip MPP SDK、本地 OpenCV fallback、MPP 库
├── third_party/         # SIP 源码压缩包
├── build/               # 构建输出和运行产物
└── build_sip_deps/      # SIP 依赖构建目录
```

### 3. 模块职责

| 模块 | 文件 | 职责 |
|---|---|---|
| 程序入口 | `main.cc` | 启动 GB28181 子系统，读取 MP4 帧 |
| 线程调度 | `push.cc` | 管理 SIP、编码、推流、队列清理线程 |
| 帧队列 | `frame_queue.h` | 有界阻塞队列，承载 `cv::Mat` 和 `H264Frame` |
| MPP 编码 | `decode.cc/.h` | YUV420 -> H.264 |
| PS/RTP 封装 | `ps_rtp.cc/.h` | MPEG-PS、PES、RTP 头生成与 UDP 发送 |
| SIP/GB28181 | `gb28181.cc/.h` | REGISTER、INVITE、MESSAGE、SUBSCRIBE 等 |
| 配置 | `config.cc/.h` | 读取 `gb28181.conf` 或使用内置默认值 |
| XML 工具 | `xml_utils.cc/.h` | 提取 SN、CmdType，UTF-8/GB2312 转换 |

### 4. 线程模型

`push.cc` 中主要线程：

```text
g_sip_thread
  -> SipThread()
  -> eXosip 初始化、监听、注册、事件循环

decode_thread
  -> main.cc 中启动
  -> OpenCV VideoCapture 读取 MP4
  -> set_Mat()

encode_thread
  -> ACK 后启动
  -> matData -> MPP encoder -> h264data

work_thread
  -> ACK 后启动
  -> h264data -> PS/RTP/UDP

check_thread
  -> checkQueueThread()
  -> matData 满 10 帧时清空，避免生产者永久阻塞
```

两个核心队列：

```text
FrameQueue<cv::Mat> matData(10)
FrameQueue<H264Frame> h264data(50)
```

`matData` 承载 OpenCV 原始帧，`h264data` 承载编码后的 H.264 帧。

### 5. main.cc：入口与视频读取

`main()`：

```text
pushToGB28181()
  -> 启动 SIP 线程

decode_from_mp4("/home/cat/project2/test.mp4")
  -> cv::VideoCapture
  -> cap.read(frame)
  -> set_Mat(frame)
  -> sleep 10ms
```

注意：MP4 路径当前硬编码为：

```text
/home/cat/project2/test.mp4
```

部署时需要改成真实路径。项目内虽然有 `project2/test.mp4`，但代码不会自动使用相对路径。

### 6. push.cc：调度中心

重要函数：

```text
pushToGB28181()
  -> 读取 GBConfig
  -> 构造 GB28Info
  -> 创建 SipThread

SipThread()
  -> eXosip_malloc()
  -> eXosip_init()
  -> eXosip_listen_addr()
  -> send_register()
  -> eXosip_event_wait()
  -> switch(event->type)

set_Mat()
  -> clone cv::Mat
  -> push 到 matData

encode()
  -> wait_and_pop(matData)
  -> resize 1280x720
  -> BGR2YUV_I420
  -> YuvtoH264()
  -> push 到 h264data

work()
  -> wait_and_pop(h264data)
  -> 首帧 splitNalus()
  -> gb28181_streampackageForH264_first()
  -> 后续帧 gb28181_streampackageForH264()
  -> RTP timestamp 每帧 +3000
```

SIP 事件与动作：

```text
EXOSIP_REGISTRATION_SUCCESS
  -> 启动 register_thread，每 30 秒重新注册

EXOSIP_REGISTRATION_FAILURE
  -> handleRegister()，处理 401 Digest 认证

EXOSIP_MESSAGE_NEW
  -> handleNews()，处理 Catalog / DeviceInfo / DeviceControl

EXOSIP_CALL_INVITE
  -> handleInvite()，解析 SDP 并回复 200 OK + SDP

EXOSIP_CALL_ACK
  -> 创建 UDP socket
  -> 启动 encode_thread
  -> 启动 work_thread

EXOSIP_CALL_CLOSED
  -> 停止编码/推流线程
  -> 关闭 UDP socket

EXOSIP_IN_SUBSCRIPTION_NEW
  -> handleSubcribe()
```

### 7. gb28181.cc：SIP 与国标信令

主要职责：

```text
send_register()
  -> 构造 REGISTER
  -> eXosip_register_send_register()

handleRegister()
  -> 如果返回 401
  -> 读取 WWW-Authenticate realm
  -> eXosip_add_authentication_info()

handleInvite()
  -> 读取 INVITE 中 SDP
  -> parseSDP()
  -> 构造 200 OK SDP
  -> eXosip_call_send_answer()

handleNews()
  -> 处理 MESSAGE
  -> DeviceInfo / Catalog / DeviceControl

handleSubcribe()
  -> 处理 SUBSCRIBE
  -> Catalog / MobilePosition

register_thread()
  -> 周期性重新 REGISTER
```

`parseSDP()` 解析字段：

```text
v=   version
o=   origin
s=   sessionName
c=   connectionAddress.ip
t=   timing
m=   media type / port / protocol / payload types
a=   rtpmap / sendonly / recvonly
y=   SSRC
```

重要注意点：

- `handleInvite()` 里的 SDP 应答 IP 当前硬编码为 `192.168.1.198`。
- `gbinfo.pushPort` 在 INVITE 处理中会被设置为对端 SDP 的媒体端口。
- 平台期望的媒体推送端口、SDP 端口、实际 UDP 发送目标必须一致。

### 8. decode.cc：MPP H.264 编码

相关类/函数：

```text
MppEncoder::instance()
MppEncoder::encode()
YuvtoH264()
test_mpp_run_yuv_init()
test_mpp_setup()
test_mpp_run_yuv()
splitNalus()
```

编码链路：

```text
OpenCV BGR
  -> push.cc resize 到 1280x720
  -> cv::cvtColor BGR2YUV_I420
  -> read_yuv_buffer()
  -> mpp_frame
  -> mpi->encode_put_frame()
  -> mpi->encode_get_packet()
  -> H264Frame
```

编码参数：

```text
codec:     H.264 AVC
profile:   Baseline profile = 66
level:     3.1
entropy:   CABAC enabled
format:    YUV420P
fps:       30
gop:       30
bitrate:   4096 * 1024
size:      当前 push.cc 固定 resize 为 1280x720
```

首帧逻辑：

```text
first_frame_flg == true
  -> 初始化 MPP encoder
  -> 编码首帧
  -> 输出通常包含 SPS/PPS/IDR
  -> first_frame_flg = false
```

`splitNalus()` 用于把首帧 H.264 packet 按 `00 00 01` 或 `00 00 00 01` 起始码切成 NALU，便于首帧特殊 PS/RTP 封装。

### 9. ps_rtp.cc：PS/PES/RTP 封装

主要函数：

```text
gb28181_make_ps_header()
gb28181_make_sys_header()
gb28181_make_psm_header()
gb28181_make_pes_header()
gb28181_make_rtp_header()
gb28181_streampackageForH264()
gb28181_streampackageForH264_first()
gb28181_send_rtp_pack()
SendDataBuff()
```

封装结构：

```text
关键帧:
  RTP header
  PS header
  SYS header
  PSM header
  PES header
  H.264 data

普通帧:
  RTP header
  PS header
  PES header
  H.264 data
```

关键参数：

```text
RTP_VERSION          = 2
RTP payload type     = 96
RTP header length    = 12
RTP packet max buff  = 1400
PES payload max      = 65522
timestamp increment  = 3000，即 90000 / 30fps
```

`gb28181_send_rtp_pack()` 会将 PS 数据追加写入：

```text
build/output.ps
```

这个文件是运行产物，不是源码。

### 10. 配置系统

相关文件：

```text
config.h
config.cc
```

`get_gb_config()` 懒加载配置。运行时会从当前工作目录读取：

```text
gb28181.conf
```

不是固定从 `project2/` 读取。

没有配置文件时使用内置默认值：

```text
devicePort    = 15060
deviceIp      = 192.168.137.70
deviceName    = 32000000001320104389

servePort     = 5060
serveIp       = 192.168.137.1
serveName     = 34020000002000000001
servePassword = 123456

pushPort      = 7000
```

配置文件格式：

```text
serveIp=192.168.137.1
servePort=5060
serveName=34020000002000000001
servePassword=123456
deviceIp=192.168.137.70
devicePort=15060
deviceName=32000000001320104389
pushPort=7000
```

支持 `#` 注释和空行。

### 11. project2 构建与运行

`project2/CMakeLists.txt` 生成：

```text
build/demo
```

依赖：

```text
OpenCV
eXosip2
osip2
osipparser2
Rockchip MPP
pthread
```

构建顺序：

```bash
cd project2
bash scripts/install_sip_local.sh
cd build
rm -rf CMakeCache.txt CMakeFiles
cmake ..
make -j$(nproc)
bash ../scripts/fix_sip_rpath.sh   # 目录移动后需要
bash ../scripts/run_demo.sh
```

`run_demo.sh` 会设置：

```text
LD_LIBRARY_PATH=project2/install/lib:$LD_LIBRARY_PATH
```

### 12. project2 注意事项

- 必须在 Rockchip ARM Linux 上运行，依赖 MPP 驱动和硬件编码器。
- `main.cc` 的 MP4 路径硬编码为 `/home/cat/project2/test.mp4`，部署时必须修改。
- `gb28181.conf` 从进程当前工作目录读取。
- `handleInvite()` 的 SDP IP `192.168.1.198` 是硬编码，实际部署应改为配置或本机真实 IP。
- UDP 本地 bind 端口在 `push.cc` 中固定为 `50099`。
- `pushPort` 默认 7000，但实际推送目标取决于平台 INVITE SDP。
- 如果 OpenCV `VideoCapture` 打不开 MP4，解码线程会结束，后续没有图像进入编码队列。
- `build/output.ps` 是运行时输出的 PS 流 dump。

---

## 三、project1 与 project2 的区别

| 对比项 | project1 | project2 |
|---|---|---|
| 主要目标 | 多路视频 AI 分析、检测、拼接、RTMP 推流 | GB/T 28181 国标 SIP 信令与 PS/RTP 推流 |
| 输入 | 本地 MP4 或 RTSP | 本地 MP4 |
| 解码 | FFmpeg 读包 + MPP 硬解码 | OpenCV VideoCapture 软件读帧 |
| AI 推理 | RKNN 多模型推理 | 无 AI 推理 |
| 图像处理 | RGA/OpenCV 转换、缩放、拼接 | OpenCV resize、BGR2YUV |
| 编码 | MPP 硬编码 H.264，用于 RTMP | MPP 硬编码 H.264，用于 GB28181 |
| 输出协议 | RTMP/FLV，RTSP 预留 | GB28181 PS over RTP over UDP |
| 信令 | 无 SIP 国标信令 | eXosip2/osip2 SIP 信令 |
| 完整主 demo | `demo_multhread_decode_infer_mulmodel` | `build/demo` |
| 运行平台 | Rockchip + RKNN/RGA/MPP | Rockchip + MPP + SIP libs |

简要理解：

```text
project1 = 视频智能分析管线
project2 = 国标平台推流管线
```

两个项目都使用 MPP，但使用位置不同：

```text
project1:
  MPP decode: 视频输入解码
  MPP encode: 拼接图 RTMP 输出

project2:
  MPP encode: MP4 帧编码为 H.264 后做国标 PS/RTP 推流
```

---

## 四、当前目录维护建议

1. 保留根目录 `project1/` 与 `project2/` 作为有效项目。
2. 不再使用 IDE 中旧路径 `项目/项目/项目/project1/...`。
3. `项目/项目/项目/project2` 已删除，避免重复副本造成误编辑。
4. `project1` 和 `project2` 的依赖库都较大，后续如果纳入版本控制，应明确哪些是源码、哪些是二进制依赖、哪些是构建产物。
5. 两个项目都有硬编码路径/IP/URL，部署前应集中配置化：
   - `project1`: 视频源、RTMP 地址、推流分辨率、启用模型列表。
   - `project2`: MP4 路径、本机 SIP IP、SDP 回答 IP、GB28181 平台参数。
